let initialState = {
		friends: [
				{name: 'Zalupkin', id: 1},
				{name: 'Zelibobik', id: 2},
				{name: 'Chert', id: 3}
		]
};

const sidebarReducer = (state = initialState) => {

		return state
}

export default sidebarReducer;