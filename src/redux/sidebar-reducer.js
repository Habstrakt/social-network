let initialState = {
		friends: [
				{name: 'Zalupkin'},
				{name: 'Zelibobik'},
				{name: 'Chert'}
		]
};

const sidebarReducer = (state = initialState) => {

		return state
}

export default sidebarReducer;